import * as constants from '../constants'

const initialState = {
  pendingTransactions: {}
}

export const transactionsReducer = (state = initialState, action) => {
  switch action.type {
    default:
    return state;
  }
}
